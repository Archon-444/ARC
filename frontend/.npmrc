# Web3 packages (MetaMask SDK, RainbowKit, etc.) have conflicting
# peer dependencies on react-native and async-storage that are
# irrelevant in a Next.js/browser environment. This flag prevents
# npm from failing on these phantom peer dep mismatches.
legacy-peer-deps=true
